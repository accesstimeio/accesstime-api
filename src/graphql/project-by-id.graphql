query ProjectById($id: BigInt!) {
    accessTimes(where: { accessTimeId: $id }) {
        id
        extraTimes
        nextOwner
        owner
        packages
        paused
        paymentMethods
        prevOwner
    }
}